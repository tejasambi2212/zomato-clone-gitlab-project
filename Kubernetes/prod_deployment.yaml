apiVersion: v1
kind: Secret
metadata:
  name: gitlab-registry-secret
  namespace: prod
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "registry.gitlab.com": {
          "username": "<username>",
          "password": "<tokan>",
          "email": "tejasambi04@gmail.com",
          "auth": "Z2l0bGFiK2RlcGxveS10b2tlbi0xNDQ0NjAxNDpnbGR0LVJyeGV3UkgyR1V6QUV0WHlFWDdT"
        }
      }
    }

--- 

apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-deployment
  namespace: prod
  labels:
    app: prod-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prod-app
  template:
    metadata:
      labels:
        app: prod-app
    spec:
      imagePullSecrets:
      - name: gitlab-registry-secret

      containers:
      - name: prod-container
        image: registry.gitlab.com/devops6080427/zomato-app:IMAGE_TAG
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "250m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name:   myapp-prod-service
  namespace: prod
spec:
  selector:
    app: prod-app
  ports:
  - port: 80
    targetPort: 80
---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-ingress
  namespace: prod
spec:
  ingressClassName: nginx
  rules:
    - host: www.zomatoapp.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: myapp-prod-service
                port:
                  number: 80

